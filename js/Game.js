/** @format */

class Game {
  constructor() {
    this.resetTitle = createElement("h2");
    this.resetutton = createbutton("");
  }

  start() {
    Player = new player();
    form = new form();
    form.display();
    tank.createSprite(width / 2 - 50, height - 100);
    tank.addImage("tank".tank.img);
    tank.scale = 0.08;

    badtanks = newGroup();
    jets = newGroup();
  }
}
